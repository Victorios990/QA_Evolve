# ── Parabank (banking — sistema público para treino) ─────────────────────────
base.url=https://parabank.parasoft.com

# ── iGaming (configurar com URL do ambiente de QA) ───────────────────────────
# Substituir pela URL real do ambiente de QA
igaming.base.url=https://igaming-qa.example.com
igaming.user=player01
igaming.password=Senha@123
igaming.admin.user=admin_qa
igaming.admin.password=Admin@Seguro1

# ── Navegador: chrome | firefox | edge ───────────────────────────────────────
browser=chrome
headless=false

# ── Banco de Dados (PostgreSQL local via Docker) ──────────────────────────────
# Para DB2 real, trocar por: jdbc:db2://host:50000/BANKING
db.url=jdbc:postgresql://localhost:5432/banking_db
db.user=qa_user
db.password=qa_password
